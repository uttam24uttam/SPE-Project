# Backend .env Template

PORT=5000
MONGODB_URI=mongodb://admin:password@mongo:27017/doctor_appointment?authSource=admin
JWT_SECRET=your_secret_key_change_in_production
NODE_ENV=development

# Database Configuration
# For production, use a proper MongoDB service like MongoDB Atlas
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/doctor_appointment

# JWT Configuration
# Change JWT_SECRET to a strong, random string in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Server Configuration
# PORT - API server port (default: 5000)
# NODE_ENV - Environment (development/production)
