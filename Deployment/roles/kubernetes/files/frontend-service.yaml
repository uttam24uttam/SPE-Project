apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: doctor-appointment
  labels:
    app: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    nodePort: 30080
    name: http
